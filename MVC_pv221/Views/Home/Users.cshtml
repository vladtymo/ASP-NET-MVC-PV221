@model List<User>;
@{
    //Layout = null;
}

<h2>User List</h2>

@if(Model.Count > 0)
{
    <ul>
        @foreach (var i in Model)
        {
            <li>[@i.Id] - @i.Login</li>
        }
    </ul>
}
else
{
    <p>No users!</p>
}